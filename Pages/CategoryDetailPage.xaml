<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MauiApp1.Viewmodels"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
             x:Class="MauiApp1.Pages.CategoryDetailPage"
             x:DataType="viewmodels:CategoryDetailVM"
             Size="300,200">

    <VerticalStackLayout Spacing="10" Padding="20">
        <Label Text="{Binding Title}" FontSize="Medium" HorizontalOptions="Center" />
        <Entry Text="{Binding Category.Description}" Placeholder="Introduce la descripción" />

        <HorizontalStackLayout Spacing="10" HorizontalOptions="CenterAndExpand">
            <Button Text="Guardar" Command="{Binding SaveCategoryCommand}" WidthRequest="100" />
            <Button Text="Cancelar" Command="{Binding CancelCommand}" WidthRequest="100" BackgroundColor="Gray" />
        </HorizontalStackLayout>
    </VerticalStackLayout>
</mct:Popup>
